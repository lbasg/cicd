name: VALIDATION STEP
on:
  workflow_dispatch:
    inputs:
      application:
        required: true
        type: string
      technology:
        required: true
        type: string  
  workflow_call:
    inputs:
      application:
        required: true
        type: string
      technology:
        required: true
        type: string
jobs:
 validation:
  runs-on: self-hosted
  steps:
  - uses: actions/checkout@v4
    with:
      repository: 'lbasg/${{ inputs.application }}'
  - name: variables
    run: |
      echo APPLICATION: ${{ inputs.application }}'
      echo TECHNOLOGY: ${{ inputs.technology }}'
      echo TAG:
